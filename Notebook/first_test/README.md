# Creating `Q1PElin` element

- Changed NodeId:
  
  ```mathematica
  "SMSNodeID" -> {"D", "D", "D", "D", "p", "p", "p", "p"},
  ```

- Changing `c` into `p`

- Starting some changes.
- Is it required to introduce coefficient and modulus of biot as material parameter?